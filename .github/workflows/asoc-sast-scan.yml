name: "HCL AppScan SAST"

permissions:
  statuses: write
  contents: read

on:
  workflow_dispatch: 
jobs:
  scan:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          
      - name: Get current date
        id: date
        run: echo "date=$(Get-Date -Format 'yyyy-MM-dd HH:mm')" >> $env:GITHUB_OUTPUT
        
      - name: Run AppScan SAST scan
        id: appscan
        uses: HCL-TECH-SOFTWARE/appscan-sast-action@main
        with:
          asoc_key: ${{ secrets.ASOC_KEY }}
          asoc_secret: ${{ secrets.ASOC_SECRET }}
          application_id: ${{ secrets.ASOC_APP_ID }}
          service_url:  ${{ secrets.ASOC_URL }}
          scan_name: "SAST ${{ steps.date.outputs.date }} Incremental scan"
          static_analysis_only: true
          sast_scan_id: 7ee8d1d5-a9dc-4229-9d06-1b1ae54699e8
          acceptssl: true
          personal_scan: true
          wait_for_analysis: false
          incremental_scan: false
          fail_for_noncompliance: false
          failure_threshold: High
    env: 
      GITHUB_TOKEN: ${{secrets.GITHUB_TOKEN}}
